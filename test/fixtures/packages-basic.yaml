schema_version: 1

packages:
  - id: alpha
    name: Alpha
    group: test
    platform:
      - macos
    tags:
      - test
    installer:
      kind: brew_formula
      package: alpha
    check:
      command: false
    config:
      target: alpha-config
      strategy: chezmoi_target
      optional: true
    manual_steps:
      - Review alpha after install.
    depends_on:
      - beta

  - id: beta
    name: Beta
    group: test
    platform:
      - macos
    tags:
      - test
    installer:
      kind: brew_formula
      package: beta
    check:
      command: false
    depends_on: []
